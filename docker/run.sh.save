y
# touch .bash_history
#
# xhost +local:
#
# docker run -it --rm \
#     --gpus all \
#     --network host \
#     -e QT_X11_NO_MITSHM=1 \
#     -e DISPLAY \
#     -e HOME \
#     -u $(id -u):$(id -g) \
#     -v /tmp/.X11-unix:/tmp/.X11-unix \
#     -v $(pwd)/.bash_history:$HOME/.bash_history \
#     -v $(pwd)/tensorflow2.0/deep-sort-yolov4:/deepsort_yolo_v4 \
#     -w /deepsort_yolo_v4 \
#     deepsort_yolo:v4
#
# xhost -local:

# https://hub.docker.com/r/tensorflow/tensorflow/


docker run -it --rm \
  --gpus all \
  --hostname $(hostname) \
  -e HOME \

  -p 8888:8888 \
  -u $(id -u):$(id -g) \
  -v $HOME:$HOME \
  -v $HOME/git/anomalous_pred:/tf/notebooks \
  anomalous_pred:jupyter
